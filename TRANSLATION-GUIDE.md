# fudan-mlsys-cn 翻译与重写指南（子代理必读）

这是对复旦大学《机器学习系统》课程网页教材（memxlife.github.io/books/mlsys/）的中文整理站点。你的任务是**把指定章节的英文原文重写为极高质量的中文**。

## 一、质量标杆

中文写作质量标杆 = `D:\_Project\mlsys-cn\docs\chapter1.md`（已重写完成）。**开始前必须先通读它**，模仿它的结构、语气、密度。

中文风格标杆 = `C:\desktoppp\pilog\blogs\posts\notes\ml\notes.md`（用户认为"极高质量、无 AI 味"）。关键特征：
- 问题驱动小标题："为什么…""什么才是…"
- 对读者说"你"，布置思想实验
- 先直觉后定义：概念出场顺序 = 直觉类比 → 一句话定义 → 要点
- 给具体数字而非形容词（"16M 元素"而非"很多"）
- 生活化类比（厨房、工地、车站），不要宏大类比
- 粗体标签词开段（`**直觉。**`、`**关键约束。**`）
- 术语首次出现带英文锚点：`算术强度（arithmetic intensity）`
- 诚实标注不确定（"这是个人猜测"）；避免斩钉截铁
- **严禁**：「首先/其次/最后/总而言之/综上所述/值得注意的是/进一步来说/这证明了/这揭示了」等模板连接词
- 每节收在小结（编号要点或一句升华）

## 二、内容要求

1. **忠实翻译原文**：保留全部小节、公式（LaTeX）、代码块、数据、人名、历史叙事。不得删减原意，不得跳过小节。
2. **修正原站已知机翻错误**（如果是重写已有中文稿）：
   - ch1："物镜在$c$中呈二次曲线" → "目标函数对$c$是二次的"；"重新安排条款收益率" → "整理后"；"屏幕点阵" → 删除或译为"该矩阵"
   - ch2："消费者物价指数"（CPI） → "每条指令的周期数（cycles per instruction）"
   - ch3："布鲁克" → Brook；"扭曲" → warp；"结束综合" → "全章小结"
   - ch4："不是哲学的，而是哲学的" → 原文 "not philosophical; governed by a hard upper bound" → "这不是哲学问题，而是被一个硬性上界支配的"
3. **接轨补充**（每章必须做）：
   - 章首用 `::: tip 本章在体系中的位置` 容器写一段：本章讲什么、在原站课程（16 周）中的位置、衔接前后章、在 MLSys/AI Infra 体系中的坐标
   - 正文按需补：与经典论文/官方文档的对照、具体硬件型号与数字、业界系统名
   - 章末加 `## 延伸阅读`：列出本章涉及的论文/官方文档链接（论文给标题+作者+年份，官方文档给名称）
4. **公式**：必须用 `$...$`（行内）和 `$$...$$`（行间），**禁止** `\(...\)` 和 `\[...\]`
5. **docsify 容器语法**：`::: tip 标题` / `::: warning 标题` / `::: info 标题`（以 `:::` 结束），对应 Obsidian 的 note/warning/example
6. **文件头**两行引用块必须保留：
   ```
   > 原文：[第 N 章](https://memxlife.github.io/books/mlsys/chapterN.html)
   > 说明：忠实翻译原网页内容，并补入与经典文献、业界系统的对照。术语首次出现给出英文锚点。
   ```
7. **标题层级**：章标题用 `# 第 N 章 中文标题`；小节沿用原文编号（如 `## 3.1 Why this chapter matters` → `## 3.1 为什么这一章重要`）

## 三、输出要求

- 直接覆盖写入 `D:\_Project\mlsys-cn\docs\chapterN.md`（或指定文件名）
- 完整输出整章，不要省略号占位
- 目标篇幅：中文约 1.5–2.5 万字符（比原文略精炼但不丢内容）
- 完成后报告：文件路径、字符数、覆盖的小节清单、补充了哪些接轨引用

## 四、可用素材

- 英文原文：`D:\_Project\mlsys-cn\.orig\chN.md`（已从原站抓取并转换为 markdown，ch1/2 是原站内嵌 markdown，其余为转换稿）
- 现有机翻（质量差，仅作对照）：`D:\_Project\mlsys-cn\docs\chapterN.md`（重写前的旧稿，会被覆盖）
- 原站章节标题结构与引用信息见各 `.orig` 文件
